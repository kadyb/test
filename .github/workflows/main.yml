on: 
  workflow_dispatch

name: test

jobs: 
  test:
    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@master
    - uses: r-lib/actions/setup-r@master
     
    - name: Install spatial dependencies
      run: brew install gdal proj

    - name: Install packages
      run: R -e 'install.packages(c("sf", "terra"))'

    - name: test script
      run: Rscript test.R
